<section class="cta" id="install">
  <div class="container">
    <p class="label">{{ $copy['cta']['label'] }}</p>
    <h2>{{ $copy['cta']['heading'] }}</h2>
    <p>{{ $copy['cta']['body'] }}</p>
    <div class="actions" style="justify-content:center">
      <a class="button button--primary" href="{{ $registerUrl }}">{{ $copy['cta']['button'] }}</a>
    </div>
  </div>
</section>
